<!-- Copyright (c) Microsoft Corporation and Contributors. -->
<!-- Licensed under the MIT License. -->

<Page
    x:Class="Singularity.Views.SongStringCollectionPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Singularity.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Margin="6 -76 6 20">
        <Grid.RowDefinitions>
            <RowDefinition Height="160"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <local:BannerView Title="{x:Bind ViewModel.Title,Mode=OneWay}"
                          Author="{x:Bind ViewModel.Author,Mode=OneWay}"
                          Thumbnail="{x:Bind ViewModel.Thumbnail,Mode=OneWay}"/>

        <ScrollViewer Grid.Row="1">
            <local:VideoIdListView
                               SongListItems="{x:Bind ViewModel.Items,Mode=OneWay}"/>
        </ScrollViewer>

    </Grid>
</Page>
