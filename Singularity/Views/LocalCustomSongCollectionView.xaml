<!-- Copyright (c) Microsoft Corporation and Contributors. -->
<!-- Licensed under the MIT License. -->

<UserControl
    x:Class="Singularity.Views.LocalCustomSongCollectionView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Singularity.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="160"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <local:BannerView Title="{x:Bind Title,Mode=OneWay}"
                          Author="{x:Bind Author,Mode=OneWay}"
                          Thumbnail="{x:Bind Thumbnail,Mode=OneWay}"/>

        <ScrollViewer Grid.Row="1">
            <local:VideoIdListView
                               SongListItems="{x:Bind SongListItems,Mode=OneWay}"/>
        </ScrollViewer>
    </Grid>
</UserControl>
